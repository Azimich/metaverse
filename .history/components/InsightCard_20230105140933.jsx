'use client';

import { motion } from "framer-motion";
import { fadeIn } from "../utils/motion";

const InsightCard = ({imgUrltitle, subtitle}) => (
  <div>
    Insight Card
  </div>
);

export default InsightCard;
